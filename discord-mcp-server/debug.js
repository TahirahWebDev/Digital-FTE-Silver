const mcpModule = require('@modelcontextprotocol/sdk/server');
console.log('MCP Module exports:', Object.keys(mcpModule));

const zodModule = require('zod');
console.log('Zod Module exports:', Object.keys(zodModule));